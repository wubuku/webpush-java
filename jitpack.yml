jdk:
  - openjdk8
before_install:
  - ./gradlew --version
install:
  - ./gradlew clean -Pgroup=com.github.wubuku -Pversion=$VERSION -xtest assemble
  - ./gradlew -b jitpack.gradle publishToMavenLocal
